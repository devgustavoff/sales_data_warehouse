version: "3.9"

services:
  db:
    image: postgres:15
    container_name: sales_data_warehouse
    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: hakkjj97
      POSTGRES_DB: sales

    ports:
      - "5434:5432"
    
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: